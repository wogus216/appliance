# 살림랩 색인 기아 진단 (2026-09-04)

/ 측정 도구: `scripts/gsc-inspect.mjs` + Search Console API(사이트맵·실적)
/ 스냅샷: `.audit/gsc/sc-domain_salimlab.kr/2026-09-04.json`

## 측정 사양

- **분석 단위**: URL 1건
- **모집단**: 2026-09-04 라이브 사이트맵의 77개 URL 전수 (표본 아님)
- **집계**: URL 검사 API의 `coverageState` 원값 분류
- **데이터 시점**: 2026-09-04. 실적은 2026-08-05 ~ 2026-09-02 (구글 확정 지연 2일 반영)
- **대조군**: 올런어바웃(allrunabout.com) — 같은 사람이 운영, 같은 방법으로 측정

## 결론 먼저

**기술적 원인은 없다.** 막힌 곳을 찾는 조사였고, 막힌 곳이 없다는 것이 결과다.

색인 요청은 **이미 작동했다.** 8/24~8/27에 요청을 넣었고 구글이 그 나흘 동안 18개를 크롤했다.
문제는 그다음이다 — 크롤한 24개 중 **23개를 색인하지 않기로 판정**했다. 홈만 색인됐다.

즉 "구글이 우리 페이지를 못 찾는" 문제가 아니라 **"보고 나서 넣지 않기로 한"** 문제다.
요청을 더 넣는 것은 같은 답을 다시 받는 일이다.

## 현황

| 상태 | 건수 |
| --- | ---: |
| 색인됨 | 1 (홈) |
| 크롤됨 · 미색인 | 23 |
| 구글이 모름 | 53 |

크롤 기록이 있는 URL은 77개 중 **24개뿐**이고, 마지막 크롤은 **2026-08-27**이다. 9월 크롤 0건.

| 크롤 날짜 | 건수 | 그날 무슨 일이 있었나 |
| --- | ---: | --- |
| 08-02 | 1 | 도메인 등록(08-01) 직후 |
| 08-14 ~ 08-22 | 5 | |
| 08-24 | 6 | 블로그 10편 배포 + 색인 요청 1일차 |
| 08-25 ~ 08-26 | 6 | 색인 요청 2일차 |
| 08-27 | 6 | 블로그 4편 배포 + 색인 요청 3일차 |

## 통과한 기술 점검 (전부 2026-09-04 실측)

| 항목 | 결과 |
| --- | --- |
| 사이트맵 등록 | 정상. 2026-08-24 제출, 구글이 **2026-09-01 내려받음**, 오류 0 · 경고 0 |
| robots.txt | `Allow: /`. URL 검사 API도 `robots=ALLOWED` |
| 색인 허용 | `indexing=INDEXING_ALLOWED` (noindex 없음) |
| 정규 URL | 홈만 슬래시 유무 차이(`https://salimlab.kr/` vs `…kr`). 실질 동일 |
| 내부 링크 | 사이트맵 77개 **전부 홈에서 2클릭 이내**. 고아 페이지 0 |
| 서버 렌더링 | Googlebot UA로 받은 HTML에 본문 있음(블로그 5,876자 · 카테고리 4,371자). JS 렌더링 의존 없음 |
| 응답 속도 | 홈 TTFB 178ms (Cloudflare) |
| 사이트맵 lastmod | 페이지별로 다른 실제 날짜가 붙음 |

**Cloudflare 관리 블록**이 robots.txt 앞에 자동 삽입돼 있다. `Content-Signal: search=yes`라
검색은 허용이고, 막는 것은 ClaudeBot·GPTBot·CCBot 등 AI 크롤러다. 검색 색인과는 무관하지만
사용자가 쓴 규칙이 아니라는 점은 알아 둘 것. `User-agent: *` 그룹이 두 번 나오는 상태이기도 하다
(구글은 같은 user-agent 그룹을 병합하므로 동작에는 영향 없음).

## 남는 설명

기술 점검이 전부 통과했으므로 원인은 사이트 신뢰도 쪽이다. 확인 가능한 사실은 둘이다.

**1. 도메인이 34일 됐다.** salimlab.kr 등록일 **2026-08-01**. 대조군 allrunabout.com은 2026-01-18
등록으로 229일이고, 773/931 색인에 30일 노출 84,533·클릭 3,709다. 같은 방법으로 만든 사이트인데
결과가 갈리는 가장 큰 차이가 나이다.

**2. 외부 링크가 없다.** 같은 사람이 운영하는 두 사이트끼리도 서로 링크가 없다(홈 기준 0건).
구글이 이 도메인을 알게 된 경로가 사이트맵 하나뿐이라는 뜻이다.

**추정:** "Crawled - currently not indexed"는 구글이 이유를 공개하지 않는 상태다. 위 둘이
지배 변수라는 것은 대조군과의 비교에 근거한 추정이며, 구글이 확인해 준 사실이 아니다.

## 하지 말 것

- **색인 요청 반복.** 8월에 이미 크롤을 불러왔고 거절당했다. 같은 페이지를 다시 요청하면
  같은 판정을 받는다. 할당량만 쓴다.
- **페이지 수 늘리기.** 77개 중 1개가 색인된 상태에서 페이지를 더 만들면 비율만 나빠진다.

## 다음 회차에 다시 잴 것

    node scripts/gsc-inspect.mjs

이전 스냅샷과 자동으로 비교해 "새로 색인·처음 크롤·색인 이탈"을 보여 준다. 봐야 할 지표는
**색인 수가 아니라 크롤 수**다. 9월에 크롤이 다시 들어오는지가 먼저고, 색인은 그다음이다.
